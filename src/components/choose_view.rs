use crate::API_CLIENT;
use crate::components::select::*;
use crate::helpers::*;
use dioxus::prelude::*;
use std::vec;
#[component]
pub fn ChooseView(space_id: String, list_id: String, view_id: Store<String>) -> Element {
    rsx! {}
}
